# Core data-pipeline dependencies (Phase 4). All free / open source.
numpy>=1.26
pandas>=2.2
xarray>=2024.1
pyproj>=3.6
PyYAML>=6.0
requests>=2.31
scipy>=1.12
h5netcdf>=1.3
h5py>=3.11
scikit-learn>=1.4
pytest>=8.0
Pillow>=10.0               # Phase 17 UI bundle: offline map PNG export (BSD-3-Clause, free)

# Optional download clients (free accounts required; imported lazily by
# backend/data_pipeline/fetch/ so the pipeline works without them):
cdsapi>=0.7              # ERA5 via Copernicus Climate Data Store
copernicusmarine>=1.4    # GLORYS12 + OSI SAF SIC/drift via CMEMS